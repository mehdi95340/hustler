<div class="container">
  <div class="add-container">
    <div class="category">
    </div>
     <%= simple_form_for [@budget, @expense] do |f| %>
    <div class="price">
        <%= f.input :amount %>
        <%= f.input :description %>
        <%= f.association :category, collection: Category.where(user: current_user)  %>
    <div class="expense-btn">
        <%= f.submit "SUBMIT", class: "btn btn-primary" %>
    </div>
    </div>
    <% end %>
  </div>
</div>
